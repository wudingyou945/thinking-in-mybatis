<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asiainfo.oto.mapper.CardMapper">

    <select id="selectCardById" parameterType="int"
        resultType="com.asiainfo.oto.pojo.Card">
        SELECT * FROM tb_card WHERE id = #{id}
    </select>
    
</mapper>